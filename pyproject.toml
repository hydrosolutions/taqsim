[project]
name = "taqsim"
version = "0.1.4"
description = "Water-modelling and rule-authoring layer over the incidence engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "networkx>=3.0",
    "pandas>=2.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "matplotlib>=3.7",
    "seaborn>=0.12",
    "plotly>=5.14",
    "deap>=1.4",
    "pyzmq>=27.1.0",
    "ipykernel>=7.1.0",
    "ctrl-freak>=0.1.0",
    "incidence",
]

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "altair>=6.0.0",
    "bump-my-version>=1.2.7",
    "marimo>=0.18.1",
    "polars>=1.35.2",
    "pytest>=8.3", # tests
    "pytest-cov>=6.2.1",
    "ruff>=0.12", # linter + formatter
]

# -----------------------
# Ruff (lint + format)
# -----------------------
[tool.ruff]
line-length = 120
target-version = "py312"
# extend-exclude = ["notebooks/", "experiments/"]

[tool.ruff.lint]
# Errors, warnings, naming, imports, modern py, bugbear, comprehensions, simplify
select = ["E", "F", "W", "N", "I", "UP", "B", "C4", "SIM"]
ignore = [
    "E501", # let formatter handle wrapping
    "N803", # allow X, Y for ML-style params
    "N806", # allow X, Y for local vars
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# -----------------------
# ty (fast static type checker)
# -----------------------
[tool.ty]
[tool.ty.rules]
possibly-unresolved-reference = "warn"

# -----------------------
# Pytest
# -----------------------
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = ["--strict-markers", "--strict-config", "-ra"]

[tool.uv]
# Keep uv's cache inside the workspace: agent gate runners sandbox children to
# workspace-write, and uv's default ~/.cache/uv is denied there.
cache-dir = ".uv-cache"

[tool.uv.sources]
incidence = { git = "https://github.com/CooperBigFoot/incidence.git", subdirectory = "bindings/python", rev = "3c67154da60b352a9077e1917dbdcb70e029b897" }
# ctrl-freak = { git = "https://github.com/hydrosolutions/ctrl-freak.git", rev = "main" } # example of how to add a git dependency
